import React from "react";

const FinishedDestinationContainer = ({ children }) => {
  return <div className="finished-destination-container">{children}</div>;
};

export default FinishedDestinationContainer;
